version: 1
services:
  - type: web
    name: wanderway-web
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    autoDeploy: true
    regions:
      - type: aws
        region: oregon
